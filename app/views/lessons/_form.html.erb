<%= render('layouts/errors', :object => @lesson) %>

<%= form_for [@course, @lesson] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <p><%= f.submit %></p>
<% end %>
